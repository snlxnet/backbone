I'm new to Android and the official docs don't have too many examples,
so I ask an LLM to generate code (saved as separate commits and clearly labelled)
and explain things.

So it's not vibe-coded (by definition, I read and edit the code),
but it's also not "100% no LLMs used"
